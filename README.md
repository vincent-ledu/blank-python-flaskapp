# Description

Blank project, with:

- app
  - hello world python flask application
  - dockerfiles with waitress configuration
- nginx
  - dockerfile with reverse proxy configuration with waitress
- docker-compose file to launch all in one
